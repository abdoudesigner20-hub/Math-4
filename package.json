{
  "name": "mathgenius-app",
  "version": "3.0.0",
  "description": "MathGenius — Application de calcul mathématique avec IA",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static app — no build needed'",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "run:android": "npx cap run android"
  },
  "keywords": ["math", "calculator", "AI", "android"],
  "author": "MathGenius Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
